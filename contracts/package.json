{
  "name": "deskill-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for DeSkill platform",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bsc-testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network bsc-mainnet",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@openzeppelin/contracts": "^4.9.3",
    "hardhat": "^2.17.1",
    "dotenv": "^16.0.0"
  }
}
